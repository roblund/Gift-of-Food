<div class="columns">
  <div class="column">
  <h1>Edit Volunteer</h1>

  <%=form_for @volunteer, :url => admin_volunteer_path(@volunteer) do |v| %>
    <%=render :partial => 'form', :locals => {:v => v} %>
  <% end %>
  </div>
</div>
